#include "UtilidadesMultilista.h"

/*Ventashechas *CrearVentasHechas(int x,int y,int z){
	Ventashechas *aux= new Ventashechas;
	aux->cedula=x;
	aux->codigo=y;
	aux->precio=z;
	aux->sig=NULL;
}

fechaVentas *CrearFechaVentas(int x){
			fechaVentas *aux= new fechaVentas;
			aux->fecha=x;
			aux->sig=NULL;
			aux->abajo=NULL;
}*/
/*CentroVentas *CrearCentroVentas(int x,int y,char nom[20],char ciu[20],char est[20],char dir[40]){
	CentroVentas *aux=new CentroVentas;
	aux->codigo=x;
	aux->telf=y;
	aux->nombre[20]=nom[20];
	aux->ciudad[20]=ciu[20];
	aux->estado[20]=est[20];
	aux->direccion[40]=dir[40];
	aux->sig=NULL;
	aux->abajo=NULL;
	aux->prox=NULL;
	return aux;
}

Clientes *CrearCliente(char nom[20], int ced, char dir[40]){
	Clientes *aux= new Clientes;
	aux->nombre[20]=nom[20];
	aux->cedula=ced;
	aux->direccion[40]=dir[40];
	aux->abajo=NULL;
	aux->sig=NULL;
	return aux;
}

productos *CrearProductos(int cod, int prec, int cant){
	productos *aux= new productos;
	aux->codigo=cod;
	aux->precio=prec;
	aux->cantidad=cant;
	aux->sig=NULL;
	return aux;
}*/ 